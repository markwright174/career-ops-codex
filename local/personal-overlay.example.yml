# Private personal overlay for Chat/Codex profile rendering.
#
# Keep a single active profile here. The helper writes the active CV to cv.md
# and can also render other personal files if those blocks are added later.

active_profile: tstc

profile:
  label: TSTC
  cv_source: cv-tstc.md
  cv_target: cv.md
  notes: Primary instructional-design-manager profile.
  # profile_yaml:
  #   candidate:
  #     full_name: Mark Wright
  #   ...
  # profile_md: |
  #   # User Profile Context -- Mark Wright
  #   ...
  # article_digest_md: |
  #   # Article Digest / Proof Points
  #   ...
